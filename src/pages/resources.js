import React from "react";
import Layout from '../components/Layout';

export default function Resources() {
	return (
        <Layout>
            <div>
                <h2>This is a list of resources I like to use.</h2>
            </div>
        </Layout>
	);
}